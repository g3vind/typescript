/***
 * 
 * ` tsc --v`
 *  for checking the version of typescript compiler
 * 
 *  */


/*
    converting into corresponding ts file
    `tsc app.ts`
*/
/*
 *  use ts-node npm module to  use just one command to compile the TypeScript file and execute the     output file in one shot
 *
 *   use command `ts-node index.ts`
 *
 *
 */

let message:string = "hello world!"
console.log(message)